{
  "name": "scfs",
  "description": "Smart Food Court System",
  "version": "1.0.0",
  "engines": {
    "node": "14.3.x"
  },
  "scripts": {
    "start": "npm --prefix client run build && npm --prefix server run build && npm --prefix server start",
    "heroku-postbuild": "npm --prefix client install && npm --prefix server install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nrcngominh/scfs.git"
  },
  "author": "NRC && HNT",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/nrcngominh/scfs/issues"
  },
  "homepage": "https://github.com/nrcngominh/scfs#readme",
  "dependencies": {
    "vue-meta": "^2.3.3"
  }
}
